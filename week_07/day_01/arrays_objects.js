emptyArray = [];
numbers = [1,2,3,4,5];

// shoppingList = ["milk", "eggs", "bread"];
// //adding to an array
// shoppingList.push("apples");
// console.log(shoppingList);  // [ 'milk', 'eggs', 'bread', 'apples' ]
// //pop() removes last element from arry
// shoppingList.pop();
// console.log(shoppingList); // [ 'milk', 'eggs', 'bread' ]
// shoppingList.pop();
// console.log(shoppingList); // [ 'milk', 'eggs' ]
// // add to the beginning of the list
// shoppingList.unshift("coffee");
// console.log(shoppingList); // [ 'coffee', 'milk', 'eggs' ]

// -value pairs (Objects)
// keys are always strings
// values can be any datatype
// can put object inside object
// JSON - Java Script Object Notation
// keys need to be unique 
ana = {
    name: "Anna",
    age: 28,
    favColour: "green",
    favFoods: ["streak", "lasagne", "shakshuka"],
    favGames: {
        nintendo: "Bayonetta",
        ps4: "God of War",
        pc: "Settlers 4"
    }

}

// to access info:
// ana["name"];
// ana.name;
// keyToAccess = "favColour",
// ana[keyToAccess]

iain = {
    name: "Iain",
    age: 25,
    favColour: "yellow"
}

zsolt = {
    name: "Zsolt",
    age: 52,
    favColour: "green"
}
// changing values
// zsolt.age = 60;

// deleting values
// delete(zsolt.age);

